- name: loops
  hosts: web
  become: yes
  tasks: 
  - name: loops
    ansible.builtin.dnf:
      name: "{{item.name}}"
      state: "{{item.state}}"
    loop:
    - {name: "nginx",state: "absent"}
    - {name: "mysql",state: "latest"}

      